name:           macmade - xcodebuild
description:    GitHub Action for xcodebuild
author:         macmade
branding:
    icon:       box
    color:      blue
runs:
    using:      'composite'
    steps:
        
        - name:     Xcode - Version
          id:       xcodebuild-version
          shell:    bash
          run:      if ${{ matrix.run-config[ 'info' ] == 1 }}; then xcode-select -p; fi
        
        - name:     Xcode - SDKs
          id:       xcodebuild-sdk
          shell:    bash
          env:
              PROJECT:    ${{ matrix.run-config[ 'project' ] }}
              SCHEME:     ${{ matrix.run-config[ 'scheme' ] }}
          run:      if ${{ matrix.run-config[ 'info' ] == 1 }}; then xcodebuild -project "$PROJECT" -scheme "$SCHEME" -showsdks; fi
        
        - name:     Xcode - Destinations
          id:       xcodebuild-destinations
          shell:    bash
          env:
              PROJECT:    ${{ matrix.run-config[ 'project' ] }}
              SCHEME:     ${{ matrix.run-config[ 'scheme' ] }}
          run:      if ${{ matrix.run-config[ 'info' ] == 1 }}; then xcodebuild -project "$PROJECT" -scheme "$SCHEME" -showdestinations; fi
        
        - name:     Xcode - Build Settings
          id:       xcodebuild-build-settings
          shell:    bash
          env:
              PROJECT:    ${{ matrix.run-config[ 'project' ] }}
              SCHEME:     ${{ matrix.run-config[ 'scheme' ] }}
          run:      if ${{ matrix.run-config[ 'info' ] == 1 }}; then xcodebuild -project "$PROJECT" -scheme "$SCHEME" -showBuildSettings; fi
        
        - name:     Xcode - Build
          id:       xcodebuild-build
          shell:    bash
          env:
              PROJECT:        ${{ matrix.run-config[ 'project' ] }}
              SCHEME:         ${{ matrix.run-config[ 'scheme' ] }}
              CONFIGURATION:  ${{ matrix.run-config[ 'configuration' ] }}
              DESTINATION:    ${{ matrix.run-config[ 'destination' ] }}
          run:      if ${{ matrix.run-config[ 'build' ] == 1 }}; then xcodebuild -project "$PROJECT" -scheme "$SCHEME" -configuration "$CONFIGURATION" -destination "$DESTINATION" -showBuildTimingSummary build CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO; fi
        
        - name:     Xcode - Analyze
          id:       xcodebuild-analyze
          shell:    bash
          env:
              PROJECT:        ${{ matrix.run-config[ 'project' ] }}
              SCHEME:         ${{ matrix.run-config[ 'scheme' ] }}
              CONFIGURATION:  ${{ matrix.run-config[ 'configuration' ] }}
              DESTINATION:    ${{ matrix.run-config[ 'destination' ] }}
          run:      if ${{ matrix.run-config[ 'analyze' ] == 1 }}; then xcodebuild -project "$PROJECT" -scheme "$SCHEME" -configuration "$CONFIGURATION" -destination "$DESTINATION" -showBuildTimingSummary analyze CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO; fi
        
        - name:     Xcode - Test
          id:       xcodebuild-test
          shell:    bash
          env:
              PROJECT:        ${{ matrix.run-config[ 'project' ] }}
              SCHEME:         ${{ matrix.run-config[ 'scheme' ] }}
              CONFIGURATION:  ${{ matrix.run-config[ 'configuration' ] }}
              DESTINATION:    ${{ matrix.run-config[ 'destination' ] }}
          run:      if ${{ matrix.run-config[ 'test' ] == 1 }}; then xcodebuild -project "$PROJECT" -scheme "$SCHEME" -configuration "$CONFIGURATION" -destination "$DESTINATION" -showBuildTimingSummary test CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO; fi
